{
  "$comment": "Hand-off pointer. EVERY work unit updates this file when it lands or blocks. `node scripts/decomp/status.mjs` reads it and warns when it lags the tree. Keep it tiny: the full story lives in decomp/game-update-slice.json and docs/decomp-port.md.",
  "next": "START: read docs/HANDOFF.md (one page, both work fronts) then run `node scripts/decomp/status.mjs` (an ERRORS block = somebody's stranded unit; fix first). STATE 2026-09-04 (recomp rounds 26-37, machineTrack has the detail; round 37 = the Chromebook budget, round 36 = music): the port BOOTS IN 3 s, RENDERS, PLAYS AUDIO AND VIDEO, and is PLAYED by an automated player (ISAAC_DRIVE=explore: rooms, pickups, combat, deaths, restarts, censused; 0 asserts over 20,000-frame soaks); two lifter defects fixed the same day (recomp_fsqrt_f64 took bits as a value; 813 goto-shaped functions started at their lowest block instead of their entry -- lift.py + lift_patches.py --entry-first); the shipping bundle is 733,800,939 bytes (37.87% of the instance, scripts/recomp/assets/bundle.py); host fastpaths for the ISAAC keystream / ArchivedFile::read / Mutex (round 27). NEXT (front B): the floor's real boss room and its trapdoor (walk there: a door preference toward the level's boss-room index; the console's debug room spawns none), WM_CHAR synthesis so typed console text works, speed beyond the leaves (the floor-generation window, PNG decode), a real browser shipping page for the bundle. Front A (hand-decomp) unchanged: Front A (hand-decomp boundaries): free removals EXHAUSTED - idx 2/3/4/5/8/11/22/35 assessed+pinned as assessment20260831* keys (read via brief.mjs <idx>). The idx-3 leaf-5 0x74f090 fold LANDED at ABI 101 (count stays 24; differential 5392 with leaf5=2p/4h coverage). BUT `brief.mjs 3` shows the live bridge delivers 0 of the 6 opaque0092f1c0* lanes, so the whole 92f1c0 try_pure ladder (v86..v101) is DORMANT in the shipped tick: the highest-value count-neutral Front-A unit is wiring the 92f1c0 receiver capture (Ready/Mode/Counter/Limit/Field14/GameType0 from Game+0x1ba78's receiver, [Game+0] at the site) into web/js/native-update-bridge.js captureUpdateLanes + bridge suite, then re-measure the tick. Other ready narrow: idx 4 mode-3 0x82eb90 fold. Front B (recomp machine track, higher leverage): ROUNDS 11-12d landed (bdf50f4..bb53dfd): the V8 wall-time profile found 95% of the 7-min boot in the guest allocator's first-fit walk; replaced (segregated free lists) -> BOOT 10.6 s, FRAME 1-3 ms, clean shutdown. ROUND 13-14a: THE GAME RENDERS AND TAKES INPUT in headless Chromium (build_boot.py --web + scripts/recomp/web/run_web.mjs -> PNG frames; input=frame:Key timelines; Enter x3 reaches FILE SELECT and the main menu). ROUND 14b-14c: the run STARTS (three lifter gaps fixed: jump-table bound, max-insns cap, data-in-code soft stops; all 26 lift failures gone). Next: read the first gameplay frames, drive the player (frame:key:hold timelines), compare Game::Update under lifted code with the hand-decomp slices; then audio (OpenAL -> Web Audio), the per-job thread design (§21.16), a live view (worker + OffscreenCanvas); the PNG chain (~7 s) remains the speed unit; the boot now runs the game's MAIN LOOP at ~135 ms/frame in the debug build (frames 4-6 of a 5-frame bounded run: 143/137/132 ms); frames 1-2 = loading screen during init, frame 3 = 418 s later (menu init + first-frame loads). Tools: ISAAC_MAX_FRAMES=N (WM_QUIT via GLFW's pump -> clean shutdown), ISAAC_LOG_TIME=1, ISAAC_STALL_DUMP=s, ISAAC_WATCH=lo:hi[:w], ISAAC_PROFILE=1 (sampling profiler, hottest lifted functions + MIPS), build_boot.py --fast (no MEM_CHECK, wasm-opt; boot-fast/). Import census counts register-held IAT loads (27 imports gained verdicts/purges); GL shim tracks renderbuffer storage; Steam accessors allow-listed; missing_fns bodies emulate ret; first clean shutdown trapped on the unlifted adjustor thunk 0x0069d1f0 (hand-written) and the 64-entry host atexit table (now 1024). NEXT: read the profiled 5-frame run (`ISAAC_MAX_FRAMES=5 ISAAC_LOG_TIME=1 ISAAC_PROFILE=1`) and the --fast run to split per-frame cost into instrumentation vs lifted work (EnterCriticalSection ~40k stub calls/frame is the first suspect), then make the per-frame STUBs real host arms (input/audio/GL draw) and do the host PNG decode (Image::LoadPNG 0x00a64a50) to cut the 11-min boot. Run FROM the instance dir. Rules in AGENTS.md; analysis docs/recomp-architecture.md §21; docs/HANDOFF.md.",
  "notes": "Open boundaries: 24 (resolved 27). HARNESS 2026-09-01 (harness round 3): scripts/decomp/lib/consistency.mjs = shared tree-consistency checks (header/model/JSON ABI agreement, cpp sizeof pins vs ABI_SIZES, JSON layout drift + canonical indent-1 form, stranded /* MUTANT */ markers + .mutation-journal.jsonl, literal ABI pins in tests, frontier freshness, uncommitted unit files) run by status.mjs (ERRORS/WARNINGS), verify-unit.mjs stage 0, tests/decomp-toolkit.test.js. scripts/decomp/slice-json.mjs check|fmt|sync = the ONLY writer of decomp/game-update-slice.json (indent 1; sync mirrors RUNTIME_INPUTS_LAYOUT/EVENTS_LAYOUT into rows - it back-filled 139 lanes, fixed 255 stale offsets, added 9 events). scripts/decomp/mutate.mjs --file --from --to -- <cmd> = crash-safe mutation check (stash+journal+/* MUTANT */ tag+sha256 restore; check|restore). verify-unit.mjs = staged gate: preflight (1 s) -> slice build once + abi.json -> everything parallel; --handoff = strict + npm test; family suites auto-detected from the dirty tree. pequery.py fieldrefs DISP [FUNC] = object-field [reg+disp] census (index decode config v2, fld table, 342,708 rows, build 48 s). brief.mjs prints live-bridge lane delivery per boundary. All 15 family test suites now pin ABI symbolically (HEADER_ABI_VERSION parsed from the .h; 700+ literals swept). Tools: status.mjs for the live picture; pequery.py for censuses - do NOT write new census scripts. Full-suite runs REQUIRE the emsdk env (verify-unit sets it itself). Live browser tick 2.25 ms (fused runtime writer); family wasm builds content-hash cached.",
  "scoutReports": "Grok 4.6 scout run 2026-08-31 (idx2/idx8 VERIFIED by the coordinator against the instruction stream 2026-08-31 and PINNED as assessment20260831 keys in game-update-slice.json — brief.mjs surfaces them; recomp fault report still unverified): output/decomp/_scratch/grok-scouts-20260831/{idx2-menu-gate-update,idx8-menu-gate-open,recomp-hud-592c-fault}.md. Verdicts: idx 8 STAYS-HOST (blocker 0x009b2150 always-path ANM2 widget Load); idx 2 NARROWED-not-removable with named further-narrow lanes (A2 latch Game+0x22ed4, FCO walk skip, tail-render byte gate, Game+0x23a78 mode capture); recomp boot fault = HUD+0x592c coop-menu image smart-ptr NULL from empty shim FS — fix by seeding resources/packed/*.a into host_shims_fs.c, do NOT fake LoadImage. | Round 2 (idx 5/11/35): idx 5 STAYS-HOST + FOUND-AND-FIXED a layout defect (frameOpaque98dba0Float2d0 0x261bc->0x2619c, ABI v99->v100, differential re-run 5387); idx 11 STAYS-HOST (blocker 0x7efa50 kind-5 arm); idx 35 scout says removable via the v139 rain voucher but UNVERIFIED (3 open questions incl. whether idx13 SFX still fires) - do not resolve-move yet. Reports in output/decomp/_scratch/grok-scouts-20260831/idx{5-98dba0,11-6fdc10,35-802a80}.md; all pinned as assessment20260831 keys.",
  "machineTrack": "ROUND 63 (2026-09-05): QUAD DRAW BATCHING, NEGATIVE -- built (batch_add/batch_flush, ENTER flushes, queries kept, attribute enables applied at the draw), pixel-identical, 3 of 50,515 draws merged: glBindTexture changes between 37,761 consecutive draws (3 in 4), glUniform2fv per draw 5,990; profiles 19.8-20.4 ms before/after (noise); edges 22/22, EDIT FILE 11/11 on it. Reverted (docs only committed). Per-draw cost = the engine's own draw order; bufferSubData 1.1-1.8 % is the floor. | ROUND 62 (2026-09-05): KEYSTREAM IN v128 -- isaac_fast_keystream_xor does 16 bytes per v128.xor under __wasm_simd128__ (a block touching r[255] goes word by word; the refill where the scalar loop puts it); selftest -msimd128 + 40-byte check across the refill; verify mode 263,360 calls. keystream 7.3 -> 5.6 % of the loading window (1.4 -> 1.0 s), the window 19.7 -> 18.2 s, first-visit frame 300 at 20.5 s. hidden from frame 63 for 10 s mid-loading: 30 frames on the 250 ms path, then frame 300 at 32.1 s, 0 errors. Browser dispatch census (3,000 frames explore): 32.1 M dispatches, cache 99.85 % hits (the 17 % slow-dispatch profiles = cold-module attribution). The one GL error 0x501 = the engine's own glDeleteProgram of a dead name (ISAAC_GL_CHECK=1), reproduced. | ROUND 61 (2026-09-05): HOST TUs WITH WASM SIMD (-msimd128 in the fast profile; lifted TUs scalar). Soak of the r60 module first: 600 s at 60 fps median, renderer WS 1,239 -> 1,022 MB, GPU 447 MB, 0 errors; edges 22/22, EDIT FILE 11/11. SIMD: nothing measurable by itself (loading window 19.7 s against 19.8, play 18.8-21.2 ms against 19.8-22.5); verify mode clean; the flag stays for a hand-written v128 keystream next. | ROUND 60 (2026-09-05): THE MEMORY MAP -- drive_memory.mjs (OS working set/private for renderer+GPU, memory-infra allocators via CDP tracing, texImage2D census; 3x HeapProfiler.collectGarbage first: raw readings vary by 500 MB with GC timing). Stage 2: renderer 1.06-1.10 GB WS / 1.47-1.51 GB private, GPU 510-520 MB; the dump sees 560 MB (partition_alloc 220-243, gpu/mapped_memory 98 = 64+32 MB transfer chunks also as shared_memory + in the GPU process, malloc 105-109, webgl 26, v8 8; wasm memory/code NOT in the dump). Reader Worker: cache cleared at frame 600 + 8 MB play budget (1,157 -> 1,091-1,101 MB vs 1,062-1,071 reader off). Image bytes dropped after placement. Uploads: 702 texImage2D, 419 MB, largest 64 MB (4096x4096) -> banded glTexSubImage2D rows of <= 4 MB (ISAAC_GL_TEX_BAND=0 = whole): pixels identical (frames 100-399 hash the same on/off), renderer working set 1,090 -> 1,002-1,004 MB, GPU process 511 -> 409-410 MB (its private bytes 607 -> 764: committed, not resident). | ROUND 59 (2026-09-05): SAVES ROUND TRIP + SHIPPED TRAIL -- drive_saves.mjs 15/15 (export zip byte-exact, import into file 2 exact before/after the reload, bare .dat into file 3; the engine's save_backups/ copies are not duplicates; one filechooser listener + queue across reloads); ship.py --trail <drive_boot boot-trail.json> -> /boot-trail.json, first visits hit 306/442; under net=200 Mbit/s frame 300 10-12 s sooner (38-40 vs 48-52 s at cpu=4); with the prefetch deferred to the first frame, 32 s against 46.5 (the first frame unchanged at 6 s); drive_memory.mjs (new, memory-infra allocators). | ROUND 58 (2026-09-05): TINFL ON THE HOST + HOST -O3 -- sub_00a85710 = miniz 1.x tinfl_decompress (archive stream inflater), ported from the public source with the 1.x details (GET_BYTE hands 0 past the input w/o flag 2; state 40 = stored-block failure; no state 54; no exit put-back/mask; state-37 test dist > dist_from_out_buf_start; byte-align skip only ahead of a zlib trailer -- verify mode caught the unconditional one: 234/39,902 DONE calls); coroutine states kept; selftest 391 (zlib-made streams whole, a byte at a time, a 512-byte ring); verify 39,902 calls 0 mismatches. Host TUs -O1 -> -O3 in the fast profile: play frame 21.0 -> 19.8 ms at cpu=4 (host share 25 -> 14 %), loading window 24.7 -> 19.8 s. Frame 300 at cpu=4: 22.3 s cold / 18.9 s warm. | ROUND 57 (2026-09-05): LOADING PROFILED -- profile_play.mjs phase=start [until=300] at cpu=4: idle 25%, keystream XOR 11.8% (byte loop -> words: 2.9 -> 1.4 s), sub_00a85710 9.5% = miniz 1.x tinfl_decompress (NEXT), sub_00adb9c0 5.8% = zlib inflate_fast (ring-buffer variant) -> host fastpath, 93,653 calls verified, 0 mismatches, same speed as the lifted body (it was C already). Frame 300 at cpu=4: 24.0 s cold / 22.3 s warm. Selftest 381, explorer md5 unchanged. | ROUND 56 (2026-09-05): THE READS ARE A WORKER'S -- isaac_fs_lazy_pread_js is a JSPI import (a promise parks the engine mid-read), a Blob Worker fetches raw bytes and transfers them, read-ahead along a file (4 windows) and the boot trail, 128 MB budget, 6 parallel; ?reader=0 = the sync path. Frame 300 at cpu=4: 27.6 s cold / 23.6 s warm vs 40.0 s with ?reader=0; waits 3.2 s / 1.0 s. Window order is scattered (head, table, then wherever the resources are). Node link fixed (isaac_editfile_gate stub outside ISAAC_WEB). edges 22 ok / 0 fail, EDIT FILE PASS 11/11, page 1 ok / 0 fail, floors PASS 21/21, the node explorer census md5 unchanged (r43 pin). NEXT: profile_play.mjs phase=start (first frame -> 300) for the loading work. | ROUND 55 (2026-09-05): THE COLD START -- 442 archive windows (440 MB) read synchronously before frame 300, 44-51 s at cpu=4; a boot trail (localStorage, written at frame 300 by the host frame counter; the present hook never fires on the served page) fetched ahead by a Blob Worker (138/442 hits: deliveries land only when the main thread yields); fetchSync decodes with Uint8Array.fromBase64 (3 ms/window vs 17): 37.9 s cold, 33.8 s warm; negative: x-user-defined sync text 70-75 s vs 50. drive_boot.mjs, bench_decode.mjs new. NEXT: the reads under JSPI answered by the Worker (raw bytes, read-ahead). | ROUND 54 (2026-09-05): EDGE HUNTS, nothing found -- 600 s browser soak (60 fps median, renderer working set 1,081 -> 1,075 MB, GPU 503 MB, 0 errors); hidden 330 s and back (22/22; the forged document.hidden covers the port's path, not Chrome's intensive throttling); drive_floors.mjs (new): stages 2-13 + 1c-4c by console, 59-60 fps each, renderer 1,075-1,193 MB after the boot transient, GPU 511-523 MB, 21/21. ROUND 53 (2026-09-05): STATIC QUAD INDEX BUFFER -- every client-array draw is a quad block in one fixed index pattern (0 2 1 1 2 3, +4 per quad), so host_gl_clientarrays.c draws them from one static ELEMENT_ARRAY_BUFFER (learnt pattern, per index type, doubled on demand): 90,760/90,760 draws over 3,000 browser frames, 0 index bytes uploaded (ISAAC_GL_QUAD_IBO=0 = the A/B; interleaved 6x profiles off 28.5 / 30.9 against on 27.2 / 26.1 ms a frame, bufferSubData 3.5 / 3.4 % against 3.1 / 2.7 % (the machine was in a slow hour; the pairs are what count)). 20,000-frame node soak: heap peak 352.7 MiB, touched span 355.2 MiB, flat. Round 52c/d on request: FPS readout = plain text, M flips it (Q was the pocket-item key), nothing in the saves store. ROUND 52 (2026-09-05): EDIT FILE -- the save-select DELETE FILE strip is EDIT FILE (page_assets.py resets the strip text in teammeatfont16bold and repacks the bundle's afterbirthp.a with the entry replaced: the last-mounted archive shadows, a loose file never wins; compact repack 442 MB, 10,228/10,228 entries verified; the instance stays pristine); a block patch at 0x9d9d59 in Menu_Save::Update (state 1 -> 2, DeleteConfirmationAppear) asks isaac_editfile_gate -> window.isaacEditFile(slot) opens the page menu (menu_overlay.mjs: EXPORT / IMPORT / DELETE / FPS VIEWER / BACK; the game's font, papers, cursor, sounds from page-assets/; window.isaacKeyCapture takes the keys; DELETE sets window.isaacEditFileDelete + isaacInjectKey(Enter) so the engine's own prompt runs); drive_editfile.mjs proves it on the dist; explorer census identical (no page = no change). ROUND 51 (2026-09-05): BUG FIXED -- the loading panel never hid (#overlay display:flex outranked the hidden attribute; global [hidden]{display:none!important}); the shipping page is the game alone (autoplay default, one bar over black, no header/hints/buttons; ?stats=1 fps line, ?saves=1 saves button, ?autoplay=0 Play button; HANDOFF reads play.html?stats=1). MEMORY MAP (memory-infra): renderer private 1.47 GB = 1,088 MiB committed wasm memory + ~450 MB Blink/GPU-mapped/malloc; guest heap touched span 355 MiB (new report line) vs 352 MiB peak; GPU process ~520 MB textures; V8 code small; no cheap lever. A host std::map::find leaf (sub_00a12280) verified bit-exact over 124,622 calls but 0.8-1.2 % vs 1.0-1.1 % lifted: reverted (a fastpath needs per-call work that dwarfs the wrapper). ROUND 50 (2026-09-05): THE WHOLE INVERSE_MDCT ON THE HOST -- sub_00aa38a0 + iter0 0x00aa30a0 / s 0x00aa3430 / ld654 0x00aa3620, transcribed from the decompile statement by statement (helper register args from the disassembly: iter0 n>>4, r n>>(l+4), s 1<<(l+1), ld654 n>>5; loop bounds l < (ld-3)>>1 and l < ld-6 with ld = ilog(n)-1); the scratch at alloc_buffer + temp_offset - n/2*4 as the original (the engine installs an alloc_buffer; the first gate refused all 2,114 calls = census 0 verified). Verify 2,114 calls 0 mismatches, census identical, selftest 372, edges 22/22; interleaved 6x profiles r49 25.2 / 21.1 -> r50 24.3 / 19.3 ms/frame (~2 % of the frame); node wall inside the noise (node decodes ~1 block/frame). LEFT: vorbis residue sub_00aa2580 0.8 % + codebook sub_00aa44d0 0.6 %; readPixels probe = GPU sync (async PBO readback = one-frame-late pixel, opt-in only if ever); bufferSubData 2.6-3.2 %. ROUND 49 (2026-09-05): LIFTED TUs AT -O3 (fast profile; node explorer wall 8695 -> 8565 ms per 3000 frames, -1.5 %, every pair of three alternating passes, census identical; the browser 10x A/B cannot resolve < 10 %: the same build gave 28.0 / 37.3 fps in consecutive passes) + HOST FASTPATH for stb_vorbis imdct_step3_inner_r_loop (0x00aa3270, bit-exact scalar SSE; verify mode 36,056 calls, 0 mismatches; selftest 361; host declarations for the lifted TUs live in recomp_rt.h) + drive_edges hidden_s=60 and save/continue (the same run via the RNG Start Seed [Continue, n] line; a continue logs no Level::Init and the room line is type.variant) + BUG FIXED: a visible page with no animation frames (the app's occluded browser pane: document.hidden false, rAF never fires) hung the yield forever -- isaac_yield_js now races rAF against a 250 ms timer (counted: Module.isaacYieldNoRaf; the status line names it; edge check 5 forges it): 22/22. 6x profile 18.9 ms/frame (r47 21.0). NEXT: the whole inverse_mdct (sub_00aa38a0 with the iter0 0x00aa30a0 / s 0x00aa3430 / ld654 0x00aa3620 helpers, host scratch for the alloca buffer, ~4 % of a frame). ROUNDS 47-48 (2026-09-05): EDGE CASES DRIVEN FOR REAL -- drive_edges.mjs (IndexedDB-seeded options.ini, typed console `stage 2` + `goto s.boss.1010`, document.hidden forged for 8 s, resume, master RMS): 16/16 on r47; BUG FIXED: the page's key table had no punctuation row (the live path dropped unnamed keys; `goto s.boss.1010` lost its dots). The engine's own glReadPixels = a 1x1 RGBA probe at (60,227) every ~12 frames (186 per 2,200 play frames) (a probe under the player, `sub_007b8cb0` through the graphics layer -- kept). Round 48: identical index blocks reused (ring generation), attribute enables and uniform re-sends skipped (107 uniform re-sends and 10 index uploads a frame gone; seeded 10x A/B 35.1/36.4 -> 38.4/37.1 fps). ROUND 45 (2026-09-05): THE COLD START -- profile_play.mjs phase=boot: first frame at 4.5 s at 4x served locally (fetchSync 30%, shader compile status queries 7.4%, atob 6%, sub_00866960 5%); drive_perf.mjs netlog=1 logs how the module is served. serve_dist.mjs re-reads dist.json on mtime change (a stale manifest served the module no-cache under a fresh ?v=). Neither Playwright's headless shell nor the app's embedded Chrome reused the HTTP cache across navigations (immutable + ETag + br; transferSize full every reload), so the code cache (round 40) stays unverifiable here; a stock Chrome on the target keeps these headers. ROUND 44 (2026-09-05): BELOW THE CAP -- drive_perf at cpu=6 measures throughput under the rAF pacing: at 10x (below the cap) round 43's module runs 33.6-34.2 fps, with the GL state filter 33.5-36.7; the 4x-5x range of a Chromebook core holds 60. host_gl_webgl.c mirrors program / active unit / texture bindings / blend func+equation / viewport and skips redundant calls (about 230 skipped native calls a frame); -O3 link = no gain at 10x (34.5 vs 34.3), stays -O2; A/B runs must pin ISAAC_EPOCH (unseeded runs split ~28/~35 fps by level). ROUND 43 (2026-09-04): GIANT FUNCTIONS SPLIT -- the run-start zone transient is the BASELINE compiler (same 1.1 GB with 2 compile tasks, 2.5 GB with --no-wasm-tier-up): Liftoff snapshots every jump target sized by the locals, and sub_005d4380 is 1.57 MB of wasm / 338k lines / 5,042 labels. split_giants.py (build-time, after every lift patch, idempotent, LIFT-SPLIT marker) rewrites functions > 60k lines into ~25k-line trampolined parts (spill + __next/__pend + return across cuts; the trampoline routes labels and consumes g_reentry_eip). Run-start V8 transient gone (allocators 631 MB at +3 s, was 1,895; Liftoff-only 448, was 2,503), renderer peak 1.22 GB (was 2.4-2.9), steady 1.05 GB; explorer run frame-identical to round 41; 58-60 fps; the six TUs compile in 56 s. ROUND 42 (2026-09-04): THE RUN-START TRANSIENT IS TURBOFAN -- drive_perf.mjs memdump=1 (Chrome memory-infra dumps, renderer allocators by effective size; raw dumps kept) shows the menu at 497 MB of allocators and play+3 s at 1,895 MB: v8/main/malloc 1,107 MB (compiler zones: hundreds of large lifted functions tier up at once on every core) + malloc/allocated_objects 365 MB, both back to baseline by +25 s. the same 1.1 GB with two compile threads and 2.5 GB with TurboFan off: the baseline compiler on the giant lifted functions (quadratic in labels x locals) -- round 43 splits them. Steady state renderer 1.1 GB + GPU 0.5 GB working set. NEXT MEMORY LEVERS: split huge lifted functions in the lifter (TurboFan graph memory is superlinear in function size); make the code cache deserialise (no tier-up for a returning player). ROUND 41 (2026-09-04): ARCHIVE WINDOWS WERE THRASHING -- the lazy-read census (drive_perf.mjs, window.isaacLazyStats) found 812 window fetches / 808 MB per run start (712 MB of afterbirthp.a; the level load reads scattered resources; stacks: fread <- sub_00a52820 <- sub_00a179c0/sub_009aa040/sub_00931050), the renderer peaking at 2.4 GB on the XHR bodies. host_shims_fs.c: 32 LRU windows per windowed file (FS_WIN_SLOTS) with a fill/hit census: 812 -> 466 windows (327 distinct) per run start; the bodies are detached after the copy, so the renderer peak fell from 2.8 GB to 1.9 GB (steady 1.1 GB). ROUND 40 (2026-09-04): THE WASM CODE CACHE -- cold headless runs are bimodal (V8 Liftoff -> TurboFan tier-up, not the dispatch cache: its census reads 99% hits); Chrome's code cache needs a cacheable response + instantiateStreaming on the fetch Response itself: run_web.mjs now serves whole files with ETag + no-cache (304), play.mjs streams the fetch Response and counts a clone for the progress bar. drive_perf/profile_play profile_dir=<dir> measure the warm start: cold 60.0 / warm 59.9 fps at 4x; Chrome stores the chunks (155-214 MB in Code Cache/wasm) but no reload deserialises them yet; the memory timeline found an 808 MB window-fetch burst per run start (round 41). ROUND 39 (2026-09-04): RINGS + TWO-WAY DISPATCH CACHE -- host_gl_clientarrays.c stages vertices/indices in ring buffers (append, 16-byte aligned, orphan with glBufferData(NULL) on wrap; offsets carried into glVertexAttribPointer/glDrawElements); mkdispatch.py's cache is 16,384 sets x 2 ways with a hit census (99.85 % in the menus, 99.35 % in a scripted run). 4x throttle: play 58.2 fps, frame work about 15 ms at the 4x throttle; steady state `isaac_lifted_dispatch` 7.5 %, `bufferSubData` 1.7 %; cold runs are bimodal (V8 tier-up, round 40). ROUND 38 (2026-09-04): THE DISPATCHER'S CACHE -- mkdispatch.py emits isaac_lifted_dispatch_cached (4096-slot direct-mapped va->id, tried by recomp_call_indirect before the shim check; off under HEARTBEAT/WATCH/TIME; the census still counts): isaac_lifted_dispatch 10.9% -> 7.3 % of a 4x-throttled frame. The yield ticks a hidden document on a 250 ms timer (rAF never fires there; the app's background pane sat at frame 4). 32.4 M dispatches in 3,000 frames still counted exactly; play 59.0 fps at 4x. ROUND 37 (2026-09-04): THE CHROMEBOOK BUDGET -- host_gl_cache.c answers glGetRenderbufferParameteriv / glCheckFramebufferStatus / glGetUniformLocation+glGetAttribLocation from what the game set (3,000 frames: renderbuffer params 26,922/0, framebuffer status 7,450/8, locations 1,230,948/32 answered by the host); the present drains glGetError every 64th frame; SwapBuffers yields through isaac_yield_js (EM_ASYNC_JS: MessageChannel, rAF-paced when the frame had spare time; NOT setTimeout (4 ms clamp) and NOT scheduler.yield (starves other tasks -- the perf driver hung)). drive_perf.mjs 4x CPU throttle + real GPU: play 60.0 fps (was 28); SwiftShader 35.6 (was 27); full CPU 60.0. Memory: renderer 1.2 GB + GPU process 0.5 GB working set. profile_play.mjs (new) is the V8 profile summariser. Selftest 354/0. ROUND 36 (2026-09-04, 29e3f6f): MUSIC PLAYS -- the WebAudio backend only got the queue head (buffer 0 on a stream the engine starts empty); now every queue/unqueue reaches the backend and a stream is a chain of AudioBufferSourceNodes; RMS proof 0.044 median on the master output (drive_audio.mjs); tests/recomp-audio.test.js 10. ROUND 34 (2026-09-04, 886a297): A HOSTABLE DIST + PLAYER PAGE -- scripts/recomp/assets/ship.py build -> .scratch/game-dist (30 files, 793,418,516 raw, 744,521,328 transfer with brotli/gzip siblings; ship.py check re-verifies), served by scripts/recomp/web/serve_dist.mjs; play.html: byte-accounted progress, Play unlocks audio, fullscreen, saves menu. Headless Chromium: Play at 0.9 s, run started, ~40 fps (software GL). ROUND 32 (2026-09-04): TYPED CONSOLE TEXT -- TranslateMessage synthesises the US-layout WM_CHAR at the queue head; ISAAC_CONSOLE_MODE=typed (default) types `stage 2` (Level::Init m_Stage 2) and `goto s.boss.1010` (Room 5.1010 Monstro); selftest 296/0. IN FLIGHT, saved as patches in the session scratchpad (r33-partial.patch: the explorer's level map / path planner / dodge / trapdoor walk, 11 tests written first; r35-partial.patch: a zlib-1.1.4 inflate_fast port + archive piece plan, 4 selftest checks failing): both agents died on a rate limit; the tree was reverted to keep it green. ROUND 30 (2026-09-04, 5a88660): THE DEBUG CONSOLE drives what the player cannot reach: options.ini EnableDebugConsole=1 seeded in the RAM-FS, commands recalled from cmd_history.txt with UP (typed text needs WM_CHAR, which the host does not synthesise); `stage 2` -> Level::Init m_Stage 2 (Basement II rooms censused), `goto s.boss.1010` -> Room 5.1010(Monstro), boss killed (TriggerBossDeath: 0 bosses remaining), boss item + hearts spawned, door reopened; the debug boss room (index -3) spawns no trapdoor by the engine's own rule. ROUND 31 (2026-09-04, cd91716): SAVES PERSIST -- the FS shim hands written files to the host on fclose (Module.isaacPersist/isaacUnlink); node ISAAC_SAVE_DIR keeps them on disk and seeds them back (2 boots: 36 persisted, 10 restored, 0 misses), the browser keeps them in IndexedDB (drive_persist.mjs: 24 persisted, 10 restored on reload). The second load had aborted in the VSync setter (glfwGetPrimaryMonitor NULL): EnumDisplayDevicesW/EnumDisplaySettingsW now describe one adapter, one monitor and one 1280x720@60 mode. Selftest 241/0, recomp suites 141/141. ROUND 29 (2026-09-04): AUTOMATED PLAYER. ISAAC_DRIVE=explore (scripts/recomp/lift/explore.mjs) reads Game/room/doors/players/NPC pool from the identity-mapped guest heap and plays door to door, hunts, sidesteps, dies and restarts. 20,000 frames, epoch 1700000000, fast node: 62 room transitions, 9 distinct rooms incl. shop/treasure/curse, 10 runs, 9 deaths, 0 asserts. Enemies spawn, move, take damage, die; the player is killed and the game-over screen leads to the next run; the rendered browser run shows the head turn and tears. Lessons: ISAAC_DISPATCH_WATCH is blind to direct calls; node has no GL so render-side fields never update. NOT exercised: pickups, trapdoor/floor descent, bosses, save/load. ROUND 28 (2026-09-04): THE SHIPPING BUNDLE -- .scratch/game-bundle is 733,800,939 bytes in 22 files, 37.87% of the 1,937,711,471-byte instance: the ten mounted archives (1,069,689,641 -> 733,456,838 bytes: lossless PNG, music at Vorbis q4 -- q3 is 9.8% smaller, not within the 5% rule -- and the entries a later mount shadows dropped, last-mount-wins read off the 0x00a17dc1 insert), resources/scripts/*.lua and savedatapath.txt; dropped by census (ISAAC_FS_TRACE + a host fs hook + the web served-files index): repentance.a, the 11 language packs, the loose tree (read 0 times), executables, run-time state. scripts/recomp/assets/bundle.py build/check/classify/table/rules (manifest .bundle.json with sha256s; tests/recomp-bundle.test.js), optimize.py music --source, run_web.mjs instance=<dir> + served_files.json, ISAAC_INSTANCE_DIR on the node driver. Proven from the bundle: node timeline (Basement, 3000 frames, 0 asserts, 39 uploads / 11 plays), node cutscene (Epilogue finished playing), browser (3001 frames, Basement, 314 uploads / 14 plays, 76.8 s); the engine's own log is identical to the pre-bundle run (523/523, 750/750 lines). ROUND 26 (2026-09-04): boot 65 s -> 2.9 s (mount checksum pass skipped by a block patch at 0x00a17cee; ISAAC_ARCHIVE_VERIFY=1 restores it). VIDEO PLAYS (ISAAC_CUTSCENE=<frame>:<id> -> Manager::ShowCutscene; libtheora's emms;ret hand-written; 12 SSE wide intrinsics implemented + Unicorn-oracled). PING-PONG + CellSpace grind ROOT-CAUSED: recomp_fsqrt_f64 was double(double) while the lifter passes bit patterns -> every sqrt 0 -> door-touch check fired from anywhere; fixed (bits in/out), 3 epochs now 0 transitions / 0 asserts before input. STRING TABLE ROOT-CAUSED: 813 goto-shaped lifted functions started at their lowest block, not their entry (Ghidra bodies absorbing a lower block; the loader's split half ran the loader's epilogue and returned 0) -> lift.py emits goto L_<entry> first, lift_patches.py --entry-first fixes the lifted tree at build time; HUD strings resolve. Also: ISAAC_EPOCH replayable floors, ISAAC_HEAP_TRACE, ISAAC_DSP_TRACE, ISAAC_ROOM_PROBE/ROOM_TEST, drive_interactive.mjs (state-driven browser driver, held keys), archive toolchain scripts/recomp/assets (verify/extract/pack/repack/optimize; lossless PNG + music q3 = 1,070 -> 750 MB), repentance.a unlisted (never mounted by this exe), windowed-read selftest. NEXT: ship the optimised archive set + drop the loose tree, fast-profile speed work (archive reader/decompressor fastpaths), gameplay depth. ROUND 24-25 (2026-09-04): AUDIO PLAYS, node and browser; the browser is interactive (JSPI). The silence was three of our own doors: every Repentance sound lives in afterbirth.a/afterbirthp.a/repentance.a (never seeded; now lazy + windowed 1 MB reads, node and web), the archive index is keyed resources/<path> and the 0x009ab970 patch had removed the resources/ root (round-10 override restored), and the 0x00a2b5c2 'branch forced' patch skipped the open of every sound source after construction (block-level lift patch + LIFT-PATCH REENTRY markers). Needed with it: guest heap 768 MiB / host base 0x34000000 (every host scratch object TEB-relative), reserve-only VirtualAlloc > 128 MiB refused, thread-handle wait = join, thread slices per frame. Node 240 s: 92-99 PCM uploads, 17-23 plays, peak heap 354 MiB. Browser: 329 uploads (42 MB, 385 s of audio), 102 plays, WebAudio running, 1,501 frames in 67.6 s incl. the 1.2 GB mount. OPEN: floor-dependent start-room ping-pong (~35 transitions at run start; replay with ISAAC_EPOCH). | ROUND 21 (2026-09-03): THE GAME PLAYS IN THE BROWSER. --web and --fast now combine (boot-web-fast; run_web.mjs fast=1). Chromium + software WebGL2: 1,500 frames in 44.9 s (debug module managed 300 in 46.6 s), all 20 scripted inputs delivered, main returned 0, and web-gameplay/frame_1500.png shows a Basement room with Isaac, HUD, minimap and two enemies. | ROUND 20 (2026-09-03): THE PORT PLAYS AT 53 FPS. Same 1,380-frame gameplay scenario: --fast profile 26 s wall, default profile 525 s -- 20x. Every gameplay grind/stall/hang measured so far was the debug instrumentation (bounds check + VA-trace store per access), not the game. ISAAC_PROFILE on the debug build: 9.0 G lifted instructions in 447 s = 21.1 MIPS, and 42% of it is two functions -- 0x00adb9c0 (23.1%) and 0x00a68cf0 (19.0%), the archive stream reader and its decompressor (zlib). MEASURE WITH --fast. | ROUND 18 (2026-09-03): the stall dump now prints 16 dwords at every pointer register, which named the grind at once (ECX = 'gfx/1000.021_Tiny Bug.anm2'). Host fastpath for the engine's path hash FUN_00a159d0 (djb2, lowercase, backslash->slash; thiscall ecx->eax): ISAAC_FASTPATH_VERIFY=1 ran 1800 frames with ZERO mismatches, and the scenario that stopped at 660 frames now reaches 900. The remaining grind moved to FUN_0041bb50, a spatial grid insert walking a rectangle of cells -- the same function as the 'CellSpace::insert: x1 > x2' assert. NEXT: the float work that computes those cell bounds. | ROUND 16d-16e (2026-09-03): the audio gate is EXACT. sub_00a9fb80 does run (reached by a direct call from the dispatched sub_00a9fa00, which is why the dispatch watch showed 0). Its sound objects have pcm=0 bytes=0 format=0 rate=0 because the sample descriptor this[0x44] is NULL and the loaded flag this[8] is 0. Class vtable 0x00ba2974: +0x04 = 0x00a9fb00 SetSampleData(ptr,len), +0x08 = 0x00a9f840 length getter, +0x2c = 0x00a9fa00 bind; 0x00a9f850 is the method that reads this[0x44] and calls +0x04. Constructor in 0x00a2b1e0 (100 bytes static / 0xA8 streaming) nulls it. REMAINING QUESTION: what attaches a sample descriptor after construction. | ROUND 16c (2026-09-03): audio traced end to end. sfx.a reads are complete and healthy (26,387 reads = the whole 25 MB, via the new ISAAC_FS_READ_TRACE). SoundEffect::Play 0x00a7cab0 runs 5x and queues, but 0x00a9fb80 (binds the AL source + alBufferData + alSourcePlay) is dispatched 0 times (new ISAAC_DISPATCH_WATCH), so Play skips its own alSourcePlay because this[0xd] is 0 and the pending list never drains. The audio thread is a DEVICE-CHANGE watcher, not a mixer (FUN_00a9e720 works only inside if(this[0x60]), whose only writer is the ALC_SOFT_system_events callback); forcing that byte ends the run. REMAINING GAP: the vtable call site that should reach 0x00a9fa00 -> 0x00a9fb80. | ROUND 16 (2026-09-03): AUDIO PIPELINE. host_audio.c = real OpenAL object model (buffers keep PCM + duration, sources have state/gain/pitch/queue and advance on the wall clock); host_audio_web.c = WebAudio backend (ISAAC_WEB only, weak hooks so node links silent); the game's mixer runs on a _beginthreadex job that never returns, so the frame present pumps one iteration of it per frame (869 iterations / 840 frames, no trap) instead of running it inline (which hangs: 10 frames in 200 s). STILL SILENT and the gate is exact: FUN_00a9fb80 needs this[10] (PCM ptr) and this[0xb] (size) non-zero and they are zero, so no alBufferData ever runs -- the sample decode between the sfx.a entry (opened 303x) and PCM is the last piece. | ROUND 15f (2026-09-03): --fast profile builds again (two decls sat inside #if RECOMP_MEM_CHECK with unconditional users: RECOMP_TICK_MASK and the host fastpath prototypes) -- 50 MB module vs 385 MB, 12,000 frames in 43 s (279 fps). NEXT BOUNDARY: the 30-min soak was a lucky run; two later runs hung just after the first room with enemies -- one after the game's own 'CellSpace::insert: x1 > x2' assert, one in a 120-entry entity-trail ring fill at 0x00942d08..0x00942d79 (FUN_00942c0e) whose capacity [esi+0x54] is zero, making both idiv divide by zero and the ring never reach 0x78. Find who writes [esi+0x54]. | SOAK (2026-09-03): 30 minutes of scripted play = 142,680 frames (79 fps), 1,115,675,915 dispatches with 0 misses and 0 block re-entries, 562 M stub calls, no stall dump, no trap, clean ISAAC_EXIT_AFTER exit. | ROUNDS 15d-15e (2026-09-03): music.a/videos.a are seeded LAZILY (the game opens them once it plays; the two 'Failed to open archive' errors are gone). That pushes the module past INITIAL_MEMORY and wasm growth copies the whole heap: 200-s play run = 900 frames at 384 MiB, 7260 at 768 MiB (new default), 7980 at 1536 MiB; --initial-memory overrides. Stub-hit recording is now an import-index table instead of a linear scan (66.5 M calls in a 10-min run). | ROUND 15c (2026-09-03): THE GAME PLAYS. The room-entry crawl was a missing dispatch-loop `case` for the function's own entry VA: block_starts() marked branch targets + the lowest body address, so a function whose body absorbed a lower range fell to default: on entry, parked a jump to itself and was re-dispatched forever with ZERO guest instructions (3.8e9 dispatches of sub_0093805f at 20 M/s, found with the new ISAAC_HEARTBEAT). Six functions had it. Fix: block_starts(body) | {start} in lift.py. Guards: check_lifted.py enforces the invariant from build_boot.py; recomp_run_pending aborts on a no-progress park cycle. Result: 13,860 frames at median 4 ms, 654 room transitions, 77.9 M dispatches, clean budget exit (was: 660 frames then stuck). NEXT: a 45-s stall early in the engine Mutex path (0x00a157f0) that recovers. | ROUND 15b (2026-09-03): 15a's 41x for --no-wasm-tier-up is WITHDRAWN -- the 145-s run was killed, not finished, and an independent flagged run stayed silent past 250 s. Real: the baseline's ~4400 s silent phase; both runs log the same 1577 stamped lines; menu frames 33 vs 38 ms median. Two harness defects fixed first: the RECOMP_VA tick was 2^20 instructions (minutes inside the crawl, so no wall-clock deadline could fire -- now RECOMP_TICK_MASK = 2^16, and the profiler math derives from it), and build_boot.py hashed only each TU's text so a recomp_rt.h edit rebuilt nothing (now folds all headers + flags into the hash, prints 'lift : dependency fingerprint'). NEXT: equal-wall-budget A/B (same ISAAC_EXIT_AFTER, with/without the flag), then Chromium tierup=0. | ROUND 15a (2026-09-03): the room-entry crawl IS TurboFan tier-up. Measured: baseline 4479 s wall / 4436 s silent, --no-wasm-tier-up 145 s / 107 s = 41x, same 1577 stamped log lines (identical guest progress); Liftoff-only also beats baseline on menu frames (33 vs 38 ms median). Levers: ISAAC_V8_FLAGS=--no-wasm-tier-up (boot_integration.mjs re-execs; NODE_OPTIONS refuses V8 flags), run_web.mjs tierup=0. OPEN: does Chromium (PartitionAlloc) crawl at all -- measure the web run next; if it does, split the giant lifted functions (0x005d4380 = 42671 insns). | ROUND 14j+ (2026-09-02): SYMBOLIZED -- the crawl's ntdll time is the NT heap allocator's free-list walk (one instruction, RVA 0x102da, 3312/3587 ticks) under node's malloc, called by V8 under the call_indirect. Windows-node specific (PartitionAlloc/glibc have no such walk). Try: eager compile, Linux/WSL or Chromium run to confirm, split giant functions. Tool: scripts/recomp/profile/prof_ntdll.py | ROUND 14j (2026-09-02): the crawl is V8 runtime time, not guest work. node --prof of the silent window: 65% ntdll under isaac_lifted_dispatch's call_indirect, 100% CPU, not paging, no compile churn, wall time nondeterministic (same dispatch count, 100 s vs hours). Candidate: main thread waiting on V8's per-module lock (lazy compiles behind background TurboFan publishes of giant lifted functions). NEXT: flag batch (--no-wasm-tier-up / --no-wasm-dynamic-tiering / --no-wasm-inlining) via scripts/recomp/profile/prof_stuck.ps1, eager compile with a long wait, split giant functions in the lifter. Tools: ISAAC_DISPATCH_TIME=1, ISAAC_EXIT_AFTER=<s>, scripts/recomp/profile/. | ROUND 14i (2026-09-02): parallel lift. lift_parallel.py --jobs 12 = 80 s wall (phase 1 55.7 s, phase 2 14.9 s) vs 1460 s sequential, byte-identical (36/38 TUs equal, the other two differ only by the build's lift_patches.py wrappers; decls/missing/every count identical). Two global decisions had to be made global: --starts-file (boundaries) and the two-phase fragment rescue (--no-rescue workers + covered.txt, one --rescue-only --covered-file pass). Pinned by tests/recomp-parallel-lift.test.js. | ROUND 14h (2026-09-02): the room-entry crawl. Stall dump = real rapidxml work; V8 profile = 61% self time in isaac_lifted_dispatch (children carry ~nothing; call_indirect inlining is off); dispatch census = 24.4 M dispatches, 0 misses (Mutex Lock/Unlock 6.5 M each via vtable); micro-benchmark ISAAC_BENCH_DISPATCH = 0.11 us/dispatch (50 ns over direct); compile-time trace = Liftoff 1.1 s / TurboFan 20.6 s on background threads -> neither the dispatcher's table nor lazy compilation explains the crawl; round 14j measures the dispatcher in place (ISAAC_DISPATCH_TIME=1). | ROUND 14g (2026-09-02): INCREMENTAL LIFTED REBUILDS. emit.py --split-va 0x30000 (TU = address bucket, 38 TUs, stable names) + build_boot.py sha256 skip (lifted_NNN.o.sha): a lifter change now recompiles only the TUs whose text changed (no-change rebuild: 0 of 38). Tools: ISAAC_STALL_EXIT=1 (profile-friendly exit at the first stall), boot_worker.mjs, run_web.mjs timeout=<ms> + hard browser kill. Shim audit (audit_shims.py, in the suite) -> 0 unaccepted gaps. | ROUND 14d (2026-09-02): TAIL-JUMP TRAMPOLINE. 'Maximum call stack size exceeded' after the first spawn = native nesting without guest frames: the lifter emitted every guest jmp that leaves a function as `sub_T(s); return;` and the spawn loop's back-edge is a computed jump to a fragment entry (cycle sub_0093805f -> recomp_jump_indirect -> recomp_call_indirect -> isaac_lifted_dispatch -> sub_0093805f). Now: the jump parks recomp_jmp_target/recomp_jmp_pending and returns; lifted direct calls, the dispatcher and the host entry run pending targets (recomp_run_pending loops recomp_call_indirect). Globals, not CpuState fields (the host struct is a prefix of the generated one -- caught by the patch's layout assertion). Diagnosis tools added: isaac_dump_va_ring (ring + guest stack walk, called from the drivers' catch), Error.stackTraceLimit=400 with collapsed runs, boot_worker.mjs (Worker with stackSizeMb). RESULT: With the trampoline, the x87 helpers and the gap shims, the headless play run passes the first entity spawn with no native stack growth and no trap: the start room loads, its entities spawn, and the run is then inside a room-entry crawl -- a 20-second rapidxml attribute-parse stall (FUN_004165a0, parse_node/parse_element recursion over a document in the guest heap) with slow asset loads around it and no frame for minutes. That crawl is round 14h's wall; the V8 profile of it is the next measurement. | ROUND 14c (2026-09-02): first room. Two more lifter gaps: (1) --max-insns 20000 dropped the entity-spawn factory 0x005d4380 (Ghidra fragments reassembled >20k insns; vtable slot 0xb63be8 -> trap) -> 100000 (argv recorded); (2) jump tables embedded in .text (0x0062a838 etc.; 0x007b8a98/0x008151bc decode as les/lds) reached by fall-through after noreturn calls failed whole functions -> discover_body ends the path (data_stops), the emitter lowers an untranslatable instruction as recomp_unreachable (soft_traps). All 26 lift failures gone. tests/recomp-jumptables.test.js pins both. RESULT: With the cap raised and the soft stops in, the tree lifts with 0 failures (23,238 functions, 42 TUs) and the spawn factory runs: the first entity spawns (Type 6, Variant 19) -- and both builds then hit V8's 'Maximum call stack size exceeded' with a flat guest stack, the subject of round 14d. | ROUND 14b (2026-09-02): STARTING A RUN found a lifter bug: jumptables.py took the nearest `cmp reg,N` before `jmp [reg*4+TBL]` as the table bound (0x009b0d7b: `cmp esi,2/je` is control flow, the table 0x009b1210 has 4 cases) -> the 4th case unlifted -> trap during Basement generation. FIX: a bound is only `cmp IDX,N` immediately followed by an unsigned jcc; else walk the table to the function end. tests/recomp-jumptables.test.js pins it (runs the classifier on the binary when tools/ is present). Re-lift recipe: emit.py with summary.json's argv into a scratch dir, patch_reentry.py, diff vs gu/, copy changed TUs, delete their .o, build_boot.py. RESULT: With the re-lifted tree the node boot is unchanged (frame 3 at 11.3 s, main returned 0) and the web play run passes the generator: the Basement is generated and the start room loads (Room 1.2). The next wall is a V8 'Maximum call stack size exceeded' while loading that room -- round 14c. | ROUND 14a (2026-09-02): INPUT WORKS -- the menus are navigable. host_shims_win.c: a real message queue (isaac_input_key/mouse_move/mouse_button; PeekMessageW polls Module.isaacInputPoll(frame,out) then pops one MSG per call; WM_KEYDOWN/UP lParam = repeat|scancode<<16|extended<<24|up bits 30,31; DispatchMessageW calls the class WndProc as a guest sub-call; GetKeyState/GetCursorPos from the same state; SetPropW/GetPropW real (GLFW's WndProc needs GetPropW(hWnd,L\"GLFW\")); WM_ACTIVATEAPP/ACTIVATE/SETFOCUS sent at the first pump; GetActiveWindow = the main window). WALL: the DirectInput 'Message' window is created LAST -- keys must target the GLFW30 window (0x00a5b7b0), not the last-created (0x00a6cef0). Web: input=420:Enter,470:Enter,520:Enter -> beta notice accepted, title passed, frame 480 = FILE SELECT, 560 = main menu. Node: ISAAC_INPUT=... same timeline. Selftest pins + 4 mutants killed; tests/recomp-web.test.js pins the shared key table. NEXT: start a run (file -> New Run -> character -> Basement) and compare Game::Update under lifted code with the hand-decomp slices. | ROUND 13 (2026-09-02): THE RECOMPILED GAME RENDERS. 13a GL census (gl_census in the fake backend: every enum WebGL2-native, 960x540, 23 programs, client arrays TRIANGLES/UNSIGNED_SHORT, premultiplied FUNC_ADD). 13b build_boot.py --web (host .web.o -DISAAC_WEB=1, ENVIRONMENT=web, MEMFS, -lGL; boot-web/) + scripts/recomp/web/{run_web.mjs (Playwright headless Chromium, SwiftShader, local HTTP server, PNG frames), boot_web.html, boot_web.mjs (same stages, lazy reads via sync XHR base64 -- Chromium strips a leading BOM from text XHR whatever the charset; page.route cannot carry a 380 MB body)}. 13c host_gl_webgl.c forwards opengl32 -> GLES3 (precision header for fragment shaders, #version stripped, client arrays via host_gl_clientarrays.c, SwapBuffers -> glReadPixels -> Module.isaacPresent, ISAAC_GL_CHECK=1). RESULT: main returned 0, 0 GL errors, 36 draws, frame 4 = main-menu paper backdrop (output/recomp/web-run/frame_0004.png, not committed). tests/recomp-web.test.js pins the two-backend contract. §21.16 documents the 3 engine threads (audio GC 5 ms, hotplug 100 ms, EOS command spinner; nothing on the menu path waits on them). NEXT: input arms (PeekMessageW queue from the page), then audio, then threads per-job, then a live view. | ROUND 12f (2026-09-02): RAM-FS hash index (FNV-1a open addressing; fs_find was strcmp x 16384 per lookup, 183 M compares just to seed) + LAZY file bytes (isaac_fs_seed_lazy(path,size); bytes fetched on first fs_file_entry via Module.isaacLazyRead through an EM_JS bridge; selftest installs a C reader). 11,197 files registered, a 5-frame boot reads 643 (31 MB); process wall 12.4 s start to exit; host heap -208 MB. Pins mutation-checked (index insert, materialise, lazy-flag clear). | ROUND 12e (2026-09-02): BOOT 416 s -> 10.6 s, FRAME 135 ms -> 1-3 ms. `node --cpu-prof` (V8 wall-time profile, self time per wasm function) put 95.4% of the boot in guest_malloc = the first-fit walk over every block (527 k mallocs x ~100 k live blocks). host_shims_heap.c now: boundary tags + footer, segregated explicit free lists (exact 8-byte classes < 512, then powers of two), two-way immediate coalescing, same guards/meter (peak 91.4 MiB of 192). Selftest pins mutation-checked (backward/forward coalescing, unlink, split). RULE (AGENTS.md): speed units start from a wall-time profile of the whole process, never the guest-instruction histogram (ISAAC_PROFILE was off by 30x). zlib cut is moot (inflate_fast 1% of wall time; scout: zlib 1.1.x with LTCG register conventions, two inlined inflateReset copies in 0x00ab2140). NEXT: profiled 600-frame run -> instance seeding (~3.5 s), then per-frame STUBs (input/audio/GL draw) as real host arms. | ROUND 12d (2026-09-02, commit bb53dfd): HOST FASTPATH VERIFIED, NO SPEEDUP. Exact host PNG unfilter / adler32 / premultiply (host_fastpath.c) behind lift_patches.py WRAP_PATCHES wrappers (ISAAC_FASTPATH=0 lifted, ISAAC_FASTPATH_VERIFY=1 both + byte compare, default host): 0 mismatches over 533 PNG decodes, yet frame 3 at 416 s vs 418-470 s. 12 MIPS + --fast no change = the boot's seconds are HOST-side wall time (shims/FS/allocator/glue) that the guest-instruction tick profiler cannot see. NEXT = `node --cpu-prof` (V8 sampling) on a 3-frame boot, self time per wasm function (scratch cpuprof_summary.mjs pattern), cut what it names. Shutdown: ~Thread terminate fixed (adoption sets Thread done flag); then the unlifted 6-byte element dtor 0x00a67fd0 (mov [ecx],0xba04ac; ret) reached via CRT __ehvec_dtor 0x00aef638 -> hand-written in missing_fns.c (pending boot confirmation). | ROUND 12 (2026-09-02, commits 13c1488..f4c4887 + pending): FRAME LOOP RUNS at ~135 ms/frame (debug build). Frame cap ISAAC_MAX_FRAMES (SwapBuffers counts, PeekMessageW posts one WM_QUIT), per-frame stamps, stall watchdog ISAAC_STALL_DUMP (RECOMP_VA tick every 2^20 insns -> VA-ring histogram), sampling profiler ISAAC_PROFILE (g_dva binary search; g_ndispatch exported by mkdispatch), build_boot.py --fast profile (RECOMP_MEM_CHECK=0, .fast.o objects, boot-fast/, wasm-opt). GL shim: renderbuffer storage table (GL_RENDERBUFFER_WIDTH/HEIGHT read-back drives RenderTarget re-creation at 0x00a18750; a 0 answer re-created a 1024^2 target every frame). gen_shims: regHeldLoads census; 27 register-held-only imports provided. Shutdown: adjustor thunk 0x0069d1f0 (add ecx,4; jmp 0x40d040) hand-written as a tail-jump in missing_fns.c; ATEXIT_MAX 64->1024 (destructors were being dropped since round 9). Threads: 3 spawns -> trampoline 0x00a7f130 -> 0x00a5a760(block{obj,fn,arg}); ISAAC_RUN_THREADS=1 runs them inline at Sleep/WaitForSingleObject (opt-in, untested). | ROUND 11b boot result: Menu Save Init passes; stops at user32!LoadImageA via `call ebx` @0x00949b03 (register-held import, census 0 sites, purge UNKNOWN -> trap). Fix = CURATED_PURGE 24 + provided shim returning 0. | ROUND 11b (2026-09-02, commit fd531c3): the Sprite::Load fault after Menu Save Init was NOT a dead string: runtime watch (ISAAC_WATCH) showed the bytes were texture premultiply output (0x00a663c0) and the stack walk put SaveSelectMenu::Init's this at [0xc72a20]+0x1dc+0x2f4 -- Menu Save Init's edi had become esi (lea esi,[edi+0x1dc]) after 0x009ef5c0 = the DLC ownership check calling ISteamApps::BIsDlcInstalled (vtable +0x1c, thiscall pops 4) x3 through the fake CSteamAPIContext whose +0x1c slot (ReleaseInterface) pops 8 -> ESP drift 12 -> epilogue popped edi from esi's slot. FIX: host_shims_steam.c steam_fake_slots ALLOW-LIST (only 0x00bf93c8/0x00c5c510 get the fake object; all other accessors read NULL = the game's own no-Steam arm; every caller tests cmp [slot],0 first). TOOLS: ISAAC_WATCH=0xLO:0xHI[:w] runtime guest-memory watch (recomp_rt.h/.c; one full lifted recompile paid), recomp_mem_fault stack walk, ISAAC_LOG_TIME=1 stamps. PROFILE: 712 s boot = 567 s PNG decode in lifted code (4096x4096 font atlases). HAZARD: guest heap layout differs run to run (time-seeded RNG). | ROUND 11 (2026-09-02, commit bdf50f4): four walls. (1) PURGE CURATION: msvcp140 ??0basic_iostream/??0basic_ostream are vbase ctors -> callee pops sb(+isstd)+hidden most_derived = 8/12 (abi-notes §1.5 said so; round 10b curated 4/8 from the decorated name and PURGE_PATCHES applied it); the lifted stringstream ctor 0x00684ce0 under-popped 4 -> its epilogue restored ebx/esi/edi one slot low (edi == cookie^ebp = 0xbb40e64e^0x0dfc4960 identified the frame). Fixed in gen_shims.py CURATED_PURGE + lift_patches.py PURGE_PATCHES (4->8, 8->12; 5 sites) + test pin + mutation checks. RULE: a curated purge must equal the callee's `ret N`. (2) INSTANCE LAYOUT: real Steam install = resources/{packed,scripts} only + afterbirth.a/afterbirthp.a/repentance.a (1.1 GB, absent here); the instance root is the ResourceExtractor dump (10,725 files/208 MB) + resources/ (485). KAGE resolver 0x00a16c60: roots last-mounted-first, ARCHIVE INDEX BEFORE the loose map per root; archive keys are 'resources/<path>' (config.a TOC: ARCH000, 24 members; djb2 0x00a159d0 seed 0x1505 + FNV 0x00a15ab0 seed 0x5bb2220e, lowercase, \\->/). A resources/ root => stale config.a players.xml (Afterbirth+ schema, bigportraitroot) shadows the loose Repentance+ file (nameimageroot) => null deref at 0x00699b81. Fix: 0x009ab970 lift patch REMOVED (canonical `xor eax,eax; ret` restored in lifted_031.c from the parked gu-2026-08-10 tree; PATCHES now empty), boot_integration.mjs seeds the whole instance tree (skips resources/packed by name, top-level packed/, mods/, exe/dll/so/ogv), FS_SLOTS 8192->16384. (3) missing_fns.c: sub_00aefca0 (float->uint32, AVX-512 vcvttss2usi path gated on [0xc7162c]>=6) + 5 siblings never emulated `ret`; 0x0098d560 (4 calls) returned esi/edi = its own return addresses to the ambush.xml loader 0x006f4740 -> 0x00834680 vector<vector<T12>>::clear walked from this=0x009b3d95 -> _Tidy big-alloc check trap. rc_ret(s) added + test. (4) mods-init 0x008fb120 walks ISteamUGC vtable +0x128/+0x12c/+0x130 through the fake Steam context (16 slots) -> NULL call; host_shims_steam.c steam_null_slots makes accessor slot 0x00c5c48c read NULL (game's own no-Steam arm at 0x008fc529). TOOLS: isaac_dump_trap_context (CRT noreturn shim: last 512 VAs + live regs + stack from ESP), recomp_mem_fault stack walk from the spilled ESP, ISAAC_LOG_TIME=1 elapsed-ms stamps. Boot = 637 s wall (profile it). STATE: stops in Sprite::Load 0x0040bd50 -> string::assign 0x0040ccd0 on path string 0x059921ac with NULL heap ptr, right after '[warn] AnmCache: cannot remove reference to <empty>' during Menu Title/Save Init. | ROUND 10b (2026-09-01): the msvcp140 C++ iostream layer landed (host_shims_msvcp.c: streambuf/basic_ios/istream/ostream/iostream members on the real MSVC x86 layout, 54 imports; abi-notes output/decomp/_scratch/msvcp140/abi-notes.md; host selftest 103->127, mutation-checked via mutate.mjs). Landing it exposed and fixed a GENERAL baked-purge bug: the lifter bakes each host-import call's stack purge INTO THE CALLER at lift time (`s->ESP += 4u + <purge>u`), and gen_shims.py's push-count sweep miscounted five __thiscall ctors (basic_ios() as 32, should be 0; iostream 8->4, ostream 12->8, _Lockit 36->4, widen 12->4), so the lifted std::stringstream ctor over-popped and mis-passed args (sb==1). Fixed WITHOUT a full re-lift: scripts/recomp/lift/lift_patches.py PURGE_PATCHES rewrites the baked constant per call site (12 sites) and drops the touched TU's object; build_boot.py recompiles only those. RULE: when you correct a purge in gen_shims.py, add its (wrong,right) to PURGE_PATCHES; `python scripts/recomp/lift/lift_patches.py --dir output/recomp/lift/gu --check` lists stale sites. NEXT UNIT (lifter side): boot now faults just after the first operator>>(istream&,string&) returns, in tokenizer 0x0067f420 at 0x0040cf50, because the CALLER's esi is a misaligned 0x0dfc4b2f (a callee-saved reg was clobbered). An ESP/reg trace (ISAAC_MSVCP_TRACE=1, logged at _Ipfx and _Unlock) PROVES operator>> is stack-balanced (frame ESP identical at entry/exit; all shim purges paired with pushed args), so the clobber is UPSTREAM in the stringstream ctor 0x00684ce0 (past the now-correct construction it copies the string + sets up the get area through more calls) or 0x0067f420 itself. Next step: callee-saved-register (ebx/esi/edi/ebp) preservation trace across 0x00684ce0's call sites. DISPROVEN earlier hypothesis: the _Lock/_Unlock tail-jump thunks 0x00aef06b/71 are BALANCED (the lifted call [vtable+N] pushes a real retaddr the thunk's single ESP+=4 pops). _Fiopen + codecvt facets (behind fstream ctor 0x009e8010) stay loud stubs. Docs recomp-architecture.md §20. | scripts/recomp/ static recompiler. ROUND 10 (2026-09-01, boot): the archive/asset wall is DOWN. Root causes were (1) four host shims on KAGE's directory-scan path (GetFullPathNameW size query returned 0 instead of the required WCHAR count; FindFirstFileW stub; FindNextFileW + wcstombs_s weak defaults with an unknown stdcall purge because the import is register-held at 0x00a17369), (2) only archives were seeded - the install's 476 loose resources/ files (.anm2/shaders/Lua/xml) are read by PATH; RAM-FS raised 512->8192 slots, per-scan cap 128->2048, (3) 0x009ab970 (creates the resources/ mount root) is one of THIS PROJECT's 19 emulator-era hand patches (55 8b ec -> 33 c0 c3; pristine in tools/isaac-ng.unpacked.exe.pre-coinit) - undone by scripts/recomp/lift/lift_patches.py (build_boot.py applies it after every lift; one TU recompiles). Boot now: 2 roots, 6 archives, 0 'Could not open', all shaders, renderbuffers, OpenAL/theora, enums.lua+main.lua run; stops at msvcp140!basic_ios<char>::basic_ios() from 0x00684d24 in 0x00684ce0 (std::stringstream(const string&)) called by the whitespace tokenizer 0x0067f420 (41 callers). NEXT UNIT: the msvcp140 iostream subset (54 imports; 5 stream ctors 0x00414330/0x00684ce0/0x008fb120/0x009036b0/0x009e8010; reference C:/Windows/SysWOW64/msvcp140.dll 14.44) or game-level overrides of the five consumers. RUN FROM THE INSTANCE DIR (host Lua libc is NODERAWFS): cd .scratch/game-instance && ISAAC_FS_TRACE=1 node ../../output/recomp/lift/boot/boot_integration.mjs ../../output/recomp/host/isaac.segs.bin main. Host-only relink ~2 min now (build_boot.py links -O0 by default: 474 s -> 8 s; --opt-link for shipping). Host selftest 82 -> 103 checks/0 fail, every new shim mutation-checked via mutate.mjs. Only CreateThread is the theoraplayer worker (0x00aab120); nothing on the init chain waits on it. ROUND 8 (lifter): wide CALLOTHER now lowers through a `recomp_otherw_*` convention (operands as pointer+size; SLEIGH passes the OLD DEST first, so a1 is the source), plus wide INT_NEGATE/INT_2COMP/SUBPIECE; lift failures 73 -> 26, .text coverage 96.44% -> 96.73%, sub_00ab2d80 lifts. Only pshuflw/pshufhw implemented in recomp_rt.c; the other 28 wide intrinsic names are weak self-naming aborts. NEW ORACLE scripts/recomp/oracle/wideops.py lifts ONE x86 instruction with the real lifter, compiles it against the real recomp_rt.c and diffs 200 random XMM vectors vs Unicorn (12 cases incl. 4 controls; tests/recomp-wideops.test.js, 2.7 s). It found a REAL SILENT MISCOMPILE: Ghidra's ia.sinc gives PSLLD/PSLLQ (xmm) a PER-LANE shift count where hardware uses the low 64 bits of the source for every lane - 12 real sites in .text, all movd-loaded counts, so lanes 1..3 were left unshifted. Fixed by packed_shift_defect() in lift.py; fires on exactly 48 lanes = 12x4, matching the byte census. REPRODUCIBILITY: emit.py records argv in summary.json; --hand-written reads the authoritative VA set out of scripts/recomp/host/src/missing_fns.c so the lifter never duplicates a hand-transcribed body; --trace-va is LOAD-BEARING (RECOMP_VA markers are what patch_reentry.py/mkdispatch.py scan; without it dispatch_tbl.c does not compile). Old Aug-10 module parked at output/recomp/lift/gu-2026-08-10. ROUND 9 (boot): boot now reaches Steam+EOS, GL 4.6.0, two SwapBuffers, libtheora/libvorbis, 31,423 heap allocs (peak 53.5 MiB - heap live for the first time), version banner Repentance+ v1.9.7.17.J460, guard intact. IT THEN FAILS TO OPEN EVERY PACKED ARCHIVE -> 33 AnmCache failures -> null ANM2 deref at 0x009a26c2. CORRECTION to an earlier note: this is NOT a re-open failure and NOT an FS-shim problem. ISAAC_FS_TRACE=1 proves the archives are never fopen'd (16 FS probes in a whole boot: save-dir setup, savedatapath.txt, log.txt, kage_mount_points.dat x2, two Lua scripts, options.ini). The decision is inside the guest: 0x00a179c0 -> 0x00a17180 -> 0x00a16c60 resolves relative paths ONLY through a per-mount-root std::map keyed by the path hash at 0x00a159d0. Read back with ISAAC_DUMP32 from the trapped run: mount-root vector [0x00c379e8]=0x00d09ca4 / [0x00c379ec]=0x00d09ca8 = exactly ONE root; that root's map at 0x00d09c30 has _Mysize == 0; loaded-archive count [0x00c37b14] == 0. The root is mounted unconditionally by 0x009abbd0 (from 0x009aa040 at 0x009aa74c) from the empty string at 0x00b1a4ec, and mounting scans nothing (0x00a16e00 is 54 insns, no FS API). NEXT UNIT, two threads: (1) what fills a mount root's map; (2) the archive list 0x009aa040 walks at [0x00bfae60], which has no writer in .text. 0x00a17180 also has an absolute drive-letter branch that bypasses the VFS - a lever if the names can be made absolute. TOOLS (use before adding a printf): ISAAC_FS_TRACE=1 logs every FS probe + answer; ISAAC_DUMP32=0xva:n prints guest dwords after main traps (guest memory is identity-mapped and the harness still holds the module, so this is free where a relink is ~7 min). ALSO FIXED: fs_key never collapsed a '.' path segment, so the game's own GetFileAttributesA(\"./\") cwd probe normalised to c:/isaac/. and missed; fixed + mutation-checked, host selftest 77 -> 82 checks 0 failures. Not the archive blocker. Full analysis docs/recomp-architecture.md §18; logs output/recomp/lift/boot/run-fix1.log (traced), run-gu2.log.",
  "updated": "2026-09-04",
  "updatedAbi": 101
}
